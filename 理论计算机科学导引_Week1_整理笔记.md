# 理论计算机科学导引 Week 1 整理笔记

## 1. 本周核心问题

理论计算机科学常关注“哪些任务可以被计算机完成，以及完成到什么程度”。第一周笔记围绕三个基本对比展开：

1. Solvable vs. Unsolvable
   - Solvable：存在算法可以解决该问题。
   - Unsolvable：不存在算法可以解决该问题。
   - 典型例子：停机问题 Halting Problem 是不可解的。

2. Decision vs. Verification
   - Decision：直接判定一个实例是否满足条件，输出 yes/no。
   - Verification：给定一个候选证明或证书，检查它是否正确。
   - 这对应复杂性理论中的 P vs. NP 问题：是否所有“容易验证”的问题也都“容易求解”，目前仍是未知。

3. Deterministic vs. Randomized
   - Deterministic：算法的每一步完全由当前状态决定，同一输入总得到同一执行过程。
   - Randomized：算法允许使用随机性，执行路径可能因随机选择不同而变化。
   - 可对应 P vs. RP 等随机化复杂性类的讨论。

## 2. 计算模型的基本抽象

可以把一个计算任务抽象为函数：

```text
f: input -> output
```

计算机的输入、输出都可以用有限长度的二进制串表示。因此通常把一般对象先编码成 `{0,1}*` 中的字符串，再讨论算法如何处理这些字符串。

## 3. 字母表与字符串

### 3.1 字母表

字母表通常记为 `Sigma`，是一个有限的符号集合。

常见例子：

```text
Sigma = {0,1}
```

### 3.2 长度为 n 的字符串集合

`Sigma^n` 表示所有长度为 `n` 的字符串：

```text
Sigma^n = Sigma x Sigma x ... x Sigma
        = {a1 a2 ... an | ai in Sigma}
```

例如当 `Sigma = {0,1}` 时：

```text
Sigma^2 = {00, 01, 10, 11}
```

### 3.3 空串

`Sigma^0 = {epsilon}`，其中 `epsilon` 表示空串，长度为 0。

### 3.4 所有有限字符串

`Sigma*` 表示字母表 `Sigma` 上所有有限长度字符串的集合：

```text
Sigma* = Sigma^0 union Sigma^1 union Sigma^2 union ...
       = union_{n >= 0} Sigma^n
```

如果 `Sigma = {0,1}`，那么 `{0,1}*` 就是所有有限二进制串的集合。

## 4. 字符串操作：拼接、前缀与后缀

### 4.1 拼接 Concatenation

若：

```text
x = a1 a2 ... an
y = b1 b2 ... bm
```

则二者拼接为：

```text
xy = a1 a2 ... an b1 b2 ... bm
```

### 4.2 前缀 Prefix

字符串 `x` 是 `y` 的前缀，当且仅当存在某个字符串 `z`，使得：

```text
y = xz
```

也就是说，`y` 可以从 `x` 开始，后面再接上一段 `z`。

### 4.3 后缀 Suffix

字符串 `x` 是 `y` 的后缀，当且仅当存在某个字符串 `z`，使得：

```text
y = zx
```

也就是说，`y` 可以由某段 `z` 开始，最后以 `x` 结尾。

## 5. 编码 Encoding

为了让计算机处理一般数学对象，需要把对象编码成二进制串。

设 `A` 是某类对象的集合，一个编码函数可以写成：

```text
E: A -> {0,1}*
```

一个好的编码通常至少需要：

1. One-to-one：不同对象编码后不能得到同一个字符串。
2. 可解码：从编码串可以唯一恢复原对象。

### 5.1 自然数的二进制编码

自然数可以直接用二进制表示：

```text
0, 1, 10, 11, 100, ...
```

但如果要连续编码多个对象，普通二进制表示会出现边界不清的问题。

例如：

```text
E(1) = 1
E(6) = 110
```

直接拼接得到：

```text
1110
```

如果没有额外规则，就不一定知道应解析成 `1,110`，还是其他分段方式。

## 6. 前缀码 Prefix-free Code

### 6.1 定义

编码 `E` 是 prefix-free 的，当且仅当对任意不同的 `a, a' in A`：

```text
E(a) is not a prefix of E(a')
```

直观地说，没有任何一个合法编码是另一个合法编码的开头。

### 6.2 为什么前缀码重要

如果编码是 prefix-free，那么连续拼接多个编码时，可以从左到右唯一切分。

因此可以自然定义：

```text
E*: A* -> {0,1}*
E*(a1, ..., an) = E(a1) ... E(an)
```

### 6.3 定理

如果 `E` 是 prefix-free 的，那么 `E*` 是 one-to-one 的。

也就是说，若把多个对象的编码直接拼接起来，结果仍然可以唯一解码。

### 6.4 证明思路

假设存在两个不同序列：

```text
(a1, ..., ak) != (b1, ..., bl)
```

但它们的拼接编码相同：

```text
E(a1) ... E(ak) = E(b1) ... E(bl)
```

比较最左端的第一个编码块 `E(a1)` 与 `E(b1)`：

- 如果二者长度相同，则相同的前缀推出 `E(a1)=E(b1)`，由 one-to-one 得到 `a1=b1`。
- 如果一个更短，那么较短者就是较长者的前缀，这与 prefix-free 矛盾。

因此只能有 `a1=b1`。去掉第一块后重复同样的论证，最终得到两个序列完全相同，与“两个序列不同”的假设矛盾。

所以 `E*` 是 one-to-one。

## 7. 构造 prefix-free 编码

笔记中给出了一种把普通二进制串变成 prefix-free 编码的方法。

若原编码为：

```text
E(a) = 010
```

可以规定：

```text
0 -> 00
1 -> 11
```

并在末尾追加终止标记：

```text
01
```

于是：

```text
E'(a) = 00 11 00 01
```

即：

```text
E'(a) = 00110001
```

这个编码是 prefix-free 的，因为中间数据位只会出现 `00` 或 `11`，终止标记是 `01`，解码时第一次遇到 `01` 就知道当前对象编码结束。

若 `|E(a)| = n`，则：

```text
|E'(a)| = 2n + 2
```

因此长度只增加常数倍和常数项，仍是可控的。

更一般地，如果希望先写出原编码的长度，也可以使用自描述编码，使总长度形如：

```text
|E(a)| + log |E(a)| + O(log log |E(a)|)
```

这表示除了原始内容外，还需要少量额外信息来描述边界。

## 8. 可数集 Countable Set

### 8.1 定义

集合 `A` 是可数的，可以用下面几个等价方式理解：

1. `A` 是有限集；或
2. 存在双射 `f: A -> N`；或
3. 存在单射 `g: A -> N`；或
4. 存在满射 `h: N -> A`。

其中 `N` 表示自然数集合。

直观上，可数集就是可以把元素排成一个列表：

```text
a0, a1, a2, ...
```

### 8.2 单射与双射的关系

如果存在单射：

```text
g: A -> N
```

则可以按照 `g(a)` 的大小对 `A` 的元素排序，从而构造一个枚举 `A` 的序列，也就得到从 `N` 到 `A` 的满射，进一步说明 `A` 可数。

## 9. `{0,1}*` 是可数的

### 9.1 引理

所有有限二进制串的集合 `{0,1}*` 是可数的。

### 9.2 证明方法

可以把二进制串按长度分组，再按字典序排列：

```text
epsilon
0, 1
00, 01, 10, 11
000, 001, 010, ...
...
```

每一层长度为 `n` 的字符串数量是有限的，总体是可数多个有限集合的并，因此 `{0,1}*` 可数。

也可以构造一个单射：

```text
g(x) =
  0, if x = epsilon
  2^{|x|} + value(x), if |x| >= 1
```

其中 `value(x)` 是把 `x` 当作二进制数得到的值。这样不同字符串会被映射到不同自然数。

## 10. 重要结论：可编码对象集合是可数的

如果一个任务或对象集合 `A` 可以被一一编码到有限二进制串中：

```text
E: A -> {0,1}*
```

且 `E` 是 one-to-one，那么因为 `{0,1}*` 是可数的，所以 `A` 也是可数的。

这为之后证明“有些问题不可解”打基础：

- 程序/算法可以编码成有限二进制串，因此程序集合是可数的。
- 但某些问题或语言的集合可能是不可数的。
- 因此一定存在无法被任何程序解决的问题。

## 11. 本周脉络总结

本周笔记的主线是：

```text
计算任务
  -> 输入输出需要编码
  -> 编码使用有限二进制串
  -> 为了拼接和解码，需要 prefix-free 编码
  -> 有限二进制串集合 {0,1}* 是可数的
  -> 可编码对象最多可数
  -> 为之后的不可解性证明做准备
```

## 12. 复习时应重点掌握

1. 能区分 solvable、unsolvable、decision、verification、deterministic、randomized。
2. 熟悉 `Sigma^n`、`Sigma^0`、`Sigma*` 的含义。
3. 会解释拼接、前缀、后缀。
4. 理解为什么普通编码在拼接时可能有歧义。
5. 理解 prefix-free 编码如何保证唯一解码。
6. 会证明：若 `E` prefix-free，则 `E*` one-to-one。
7. 会说明 `{0,1}*` 为什么是可数的。
8. 理解“可编码对象集合是可数的”这一结论为什么重要。
